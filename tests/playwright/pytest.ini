[pytest]
# Rootdir anchor for the Playwright E2E test suite.
#
# This file pins pytest's rootdir to tests/playwright/ so that pytest does NOT
# traverse up into the HA component package (/app/__init__.py), which imports
# HA-specific packages (voluptuous etc.) that are not installed in the test
# runner image.
